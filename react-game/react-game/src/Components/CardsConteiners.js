import React, {useState} from 'react';



/*
 class CardConteinerss extends React.Component {

 constructor(props) {
  const {card, setCard} = useState();
  super(props);
  this.state = {count: ''};
  this.count = 0;
}


pushImg(event){
 Cards1.push(this.count);
 this.setState({count: this.count});
 this.count+=4;
 console.log(card);
}

 render() {
  return (
   <div className="CardConteinters">
     <div className="cardCont1" id='cont1' onClick={this.pushImg.bind(this)}>{
      this.count !== this.state.count ? 
      Cards1.map((e,i) => <img key={i} id='cont1' src="./cards/22.png" style={{top:`${e}rem`, left:`${-e/8}rem`}}/>) : ''
     }</div>
     <div className="cardCont2">2</div>
     <div className="cardCont3">3</div>
     <div className="cardCont4">4</div>
  </div>
  )
 }
}
*/